NABU Unconfirmed Users Deletion Confirmation
=========================================

Deletion Date: <%= Time.now.strftime('%F %T') %>

This email confirms that <%= @deleted_users.size %> unconfirmed user accounts have been deleted.

These accounts were unconfirmed for more than <%= DeleteUnconfirmedUsersJob::AGE.inspect %> and were not referenced elsewhere in the system.

List of Deleted Users:
---------------------
<% @deleted_users.each do |user| %>
ID: <%= user[:id] %>
Name: <%= user[:name].html_safe[0..20] %>
Email: <%= user[:email] || 'No email' %>
Created: <%= user[:created_at].to_date %> (<%= (Date.current - user[:created_at].to_date).to_i %> days ago)

---

<% end %>

This is an automated message from NABU. Please do not reply to this email.
