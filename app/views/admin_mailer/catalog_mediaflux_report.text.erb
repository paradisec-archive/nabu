This report validates that every file in the S3 catalog bucket is backed up in mediaflux.

missing: <%= @missing.size %>
size mismatch: <%= @size_mismatch.size %>

## Files missing from mediaflux (<%= @missing.size %>)

<% @missing.each do |file| -%>
  - <%= file %>
<% end -%>

## Files with size mismatch (<%= @size_mismatch.size %>)

<% @size_mismatch.each do |entry| -%>
  - <%= entry[:path] %> (S3: <%= entry[:s3_size] %>, Mediaflux: <%= entry[:mediaflux_size] %>)
<% end -%>
