$(document).ready ->
#  $('button.per_page').click ->
#    per_page = $(this).data('per')
#    param_name = $(this).data('param-name') || 'per_page'
#    new_search = $.query.set(param_name, per_page)
#    if ($(this).text().indexOf('all') != -1)
#      new_search = new_search.set('page', '1')
#    window.location.search = new_search
